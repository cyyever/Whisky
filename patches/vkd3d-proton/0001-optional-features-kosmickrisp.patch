diff --git a/libs/vkd3d/device.c b/libs/vkd3d/device.c
index c227db7..0a53df1 100644
--- a/libs/vkd3d/device.c
+++ b/libs/vkd3d/device.c
@@ -3327,8 +3327,9 @@ static HRESULT vkd3d_init_device_caps(struct d3d12_device *device,
 
     if (!single_storage_texel || !single_uniform_texel)
     {
-        ERR("Lacking support for single texel alignment.\n");
-        return E_INVALIDARG;
+        /* KosmicKrisp/Metal experiment: proceed despite coarser texel buffer
+         * alignment (texel buffer views may misbehave) rather than failing. */
+        WARN("Lacking support for single texel alignment; proceeding anyway.\n");
     }
 
     /* Disable unused Vulkan features. The following features need to remain enabled
